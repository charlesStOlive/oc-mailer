# ===================================
#  Duplicate Behavior Config
# 
# ATTENTION LE TITRE NE DOIT PAS AVOIR D ESPACE

# Model Class name
modelClass: Wcli\Crm\Models\WakaUser
workflow: false

###TOOLBAR
tool_bar:
    base:
        create: 
            label: waka.mailer::wakamail.create
            show: true
        reorder: 
            show: true
            url: waka/mailer/wakamails/reorder
        delete: 
            show: true
        mode: no_group
        btns: 
###BREADCRUMP
breadcrump:
    inSettings: true
    indexUrl: waka/mailer/wakamails/index/wakamails
    indexlabel: waka.mailer::wakamail.title
    title: waka.mailer::wakamail.form_name

###ACTION BAR (dans les pages update)
#Les behavior liée aux boutons doivent êtres instanciés implements = [waka.xxx.behaviors.yyy] dans le controller. 
action_bar:
    mode: no_group
    # mode: all #Va créer un unique bouton avec une liste
    # mode: group_type # Group les boutons si demandé (_group: word)
    # mode: no_group # aucun regroupement meme si demandé
    btns:
        duplicate:
            config: waka.utils::btns.duplicate


          
    
        



